class Model {
  String email, phone, pass, cpass, name, key;
  Model(
      {required this.email,
      required this.pass,
      required this.name,
      required this.key,
      required this.cpass,
      required this.phone});
}
